package game.uiSys.sign.model {
    import core.ecs.CSystem;
    
    import game.uiSys.root.mvc.CUIModel;
    
    public class CSignModelHandler extends CUIModel {
        public function CSignModelHandler(sys:CSystem) {
            super(sys);
        }
    }
}

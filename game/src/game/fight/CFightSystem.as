package game.fight {
    import core.ecs.CSystem;
    
    import game.CAppFacade;
    
    public class CFightSystem extends CSystem {
        public function CFightSystem(facade:CAppFacade) {
            super(facade);
        }
        
    }
}
